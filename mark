tee -a $(grep -rl "$1" index) <<EOF


================================================================================
This ip belongs to:
EOF
cat | tee -a $(grep -rl "$1" index)



tee -a $(grep -rl "$1" index) <<EOF


================================================================================


EOF

